{
  "name": "zweicheck",
  "version": "0.3.0",
  "private": true,
  "description": "ZweiCheck – echte Konten, Vertrauensverbindungen und Prüfanfragen",
  "scripts": {
    "start": "node server/index.js",
    "migrate": "node server/migrate.js",
    "check": "node --check app.js && node --check sw.js && find server tests -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.1",
    "helmet": "^8.3.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.3",
    "pg": "^8.22.0"
  }
}
