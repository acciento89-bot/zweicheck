{
  "name": "zweicheck",
  "version": "0.7.0",
  "private": true,
  "description": "ZweiCheck – einfache menschliche Zweitmeinung für sichere Entscheidungen",
  "scripts": {
    "start": "node scripts/patch-server-push.js && node server/index.js",
    "migrate": "node server/migrate.js",
    "check": "node --check app.js && node --check draft-guard.js && node --check deep-link.js && node --check push-client.js && node --check activity-center.js && node --check trust-routing.js && node --check escalation-client.js && node --check sw.js && find server tests scripts -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test",
    "generate-vapid": "node scripts/generate-vapid.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.1",
    "helmet": "^8.3.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.3",
    "pg": "^8.22.0",
    "web-push": "^3.6.7"
  }
}
